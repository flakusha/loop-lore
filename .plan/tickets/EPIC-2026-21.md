<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 Loop Lore Contributors -->

# EPIC-2026-21: 2026-21: Notification Expansion

**Status:** Done
**Priority:** medium
**Effort:** Medium
**Summary:** In-app notification expansion ships per-event thresholds (per-`NotificationType` enabled toggle) and a noise-preset surface (`DEFAULT_PREFS`) merged with per-user overrides plus a `mutedWorlds[]` scope filter. The `NotificationService` (`src/notifications/service/service.ts`) is the canonical in-app delivery seam; it is referenced by `epic-auth-channel-provisioning.md:28` as the upstream of future outbound channels.
**Context:** Roadmap line "Noise presets, per-event thresholds" maps to `NotificationPreferences.enabled: Record<NotificationType, boolean>` + `DEFAULT_PREFS` (`src/notifications/service/types.ts:21-39`); `prefs.ts:8-31` merges user overrides; `crud.ts:33-56` short-circuits `createNotification` when the type is disabled or the world is muted. Triggers (`src/notifications/service/triggers.ts`) cover mention/chat-invite/quest-update/gm-action/system/blog-post/blog-comment. REST surface at `src/routes/notifications/index.ts`; SSE stream at `src/routes/notifications/stream.ts`. Audit: `.tmp/audit-epic-2026-21.md`.
**Acceptance Criteria:**
- Per-event type can be enabled/disabled per user (`NotificationType` boolean map). OK
- Defaults (`DEFAULT_PREFS`) act as the noise preset; user prefs merge on top. OK
- Muted-worlds list suppresses notifications scoped to that world. OK
- CRUD + read/unread + SSE stream + recent-events block all functional. OK
- Refinement BUGs (`BUG-nsfw-modservice-notify-user-leaks-admin-reason`, `BUG-chat-mention-silent-error-swall`, `BUG-group-chat-mention-prefix-collision`) tracked independently; not epic-blocking.


**Status**: closed
**Priority**: medium
**Labels**: epic, notifications
**Epic**: (if applicable)
**Related**: git issue c4b708e

> Closed 2026-09-23 after audit confirmed noise-preset + per-event-threshold roadmap items ship on dev. Refinement BUGs continue as separate tickets.

git issue: c4b708e
