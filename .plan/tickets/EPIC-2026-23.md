<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 Loop Lore Contributors -->

# EPIC-2026-23: 2026-23: Assistant Commands

**Status:** In Progress
**Priority:** medium
**Effort:** Medium
**Summary:** The command parser + registry + 40+ handlers ship on dev. `/improve`, `/translate`, `/rewrite` are LLM-first with documented local fallback (`BUG-assistant-improve-translate-rewrite-never-call-llm.md` ✅ Resolved 2026-09-07). `/quest`, `/roll`, `/summarize`, `/impersonate`, `/narrate` are fully functional. Two roadmap gaps remain: `/image` is action-dispatch only (no backend adapter; owned by `epic-image-edit`), and slash-command typed-token autocomplete is not implemented (composer currently uses UI buttons + `@`-mention autocomplete).
**Context:** Parser at `src/assistant/command-parser.ts`. Registry + dispatch at `src/assistant/commands/registry.ts:103-141`. `/roll` at `src/assistant/commands/dice.ts:65-156`. `/quest` at `src/assistant/commands/quest.ts`. `/improve` at `src/assistant/commands/improve.ts:29-78` — `improveOrPolish` (AUX LLM) with `polishText` fallback; tests 5/5. `/translate` at `src/assistant/commands/translate.ts:97-122` — `deps.complete` via `resolveProvider` with identity fallback; tests 8/8; placeholder guard at `translate.test.ts:99-105`. `/rewrite` at `src/assistant/commands/rewrite.ts:50-124` — `complete` dep + per-style system prompt with regex fallback; tests 12/12. `/image` at `src/assistant/commands/image.ts` (31 lines) — action-dispatch only (no backend). Autocomplete: `@`-mention wired at `src/frontend/alpine/chat-group.ts:79-110`; slash-command completion is UI-button driven (`epic-assistant-generation-extensions.md:436-438`). Audit: `.tmp/audit-epic-2026-23.md`.
**Acceptance Criteria:**
- Command parser identifies `/...` and dispatches to a registered handler. OK
- 40+ slash commands registered and routed through `messages.ts:543`. OK
- `/improve` invokes the LLM rewrite pipeline via `improveOrPolish`. OK (tests 5/5)
- `/translate` invokes the LLM translation pipeline via `deps.complete`. OK (tests 8/8)
- `/rewrite` invokes the LLM rewrite pipeline per-style. OK (tests 12/12)
- `/quest` issues a quest via `src/rpg/quests.ts`. OK
- `/image` reaches a real image-generation pipeline (ComfyUI primary, sd.cpp secondary). Pending — TASK-assistant-generation-extensions + epic-image-edit
- Slash-command typed-token autocomplete (composer surfaces `/foo` candidates as the user types). Pending — no task ticket exists yet; track as new child


**Status**: open
**Priority**: medium
**Labels**: epic, assistant
**Epic**: epic-assistant-generation-extensions, epic-assistant-gm-flows, epic-image-edit
**Related**:
- git issue 8c07e16
- BUG-assistant-improve-translate-rewrite-never-call-llm (✅ Resolved 2026-09-07)
- TASK-assistant-generation-extensions (`/image` backend adapter; ComfyUI/sd.cpp routing)
- TASK-assistant-creative-studio-workflows (`/image` dispatch + creative studio runner)
- TASK-assistant-command-execution-intent-detection (🟡 Partial — AUX classifyIntent timeout/apiKey; detectIntent dead)
- TASK-assistant-commands-extension (extension surface)
- TASK-fast-action-prompt-pipeline, TASK-fast-action-utility-catalog (flow-modifier commands)
- TASK-assistant-entity-access-{character-item-inventory,asset-access,world-location-access,rag-access,modification-addition} (entity-access slash commands)
- TASK-assistant-capability-disclosure (`/capabilities`)
- TASK-assistant-analyze-slash-command-over-prompt-analysis-service (`/analyze`)
- TASK-assistant-slash-command-typed-token-autocomplete (NEW — typed-token `/` autocomplete; mirrors chat-group.ts:79-110 `@`-mention pattern)

> 2026-09-23 audit: parser + registry + 40+ handlers ship; `/improve`/`/translate`/`/rewrite` are LLM-first (BUG resolved); `/quest` + `/roll` complete. Two real gaps remain: `/image` backend adapter (TASK-assistant-generation-extensions) and slash-command typed-token autocomplete (no existing ticket — see Related). Held open until those close or are formally descoped. Audit: `.tmp/audit-epic-2026-23.md`.

git issue: 8c07e16
