# TASK-2026-015: Fix DB migration test failures (missing down + schema sync)

**Status**: open
**Priority**: high
**Labels**: tests, migrations, db
**Assignee**:
**Epic**: epic-testing-qa
**Related**: TASK-schema-migration

## Summary

DB migration tests fail — missing `down` migrations and schema-sync drift between migrations and generated schema artifacts.

## Context

Tracked in `epic-testing-qa.md` Linked Tasks table. Migration tests exercise up/down paths; missing down migrations + stale generated schemas (`schema-*.ts`, `schema-manifest.ts`) cause failures.

## Acceptance

- [ ] All migrations have matching `down` implementations
- [ ] `db:schemas:check` green (generated artifacts in sync with migrations)
- [ ] Migration test suite passes
