# Documentation configuration example (YAML)
# Domain: docs — in-app documentation panel
# Twin of config.docs.example.toml

docs:
  enabled: true

  # Optional: restrict visible docs to specific paths
  # public: ["guide/", "reference/"]
