.hermes
.tmp

dist/
node_modules/

# Runtime data (stale location — default moved to sibling ../loop-lore-data/)
data/*.db
data/uploads/

# Pre-compressed static assets (generated on server start)
*.br
*.gz
*.zst

# Environment variables (per SECURITY section in AGENTS.md)
.env
.env.*
!.env.example

# Lock files (generated, can be recreated with bun install)
bun.lock
