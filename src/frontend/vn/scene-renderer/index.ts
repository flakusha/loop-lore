/**
 * VN Scene Renderer — Barrel export
 */

export {
  addScene,
  destroyVnRenderer,
  getCurrentSceneIndex,
  getSceneCount,
  initVnRenderer,
  jumpToScene,
  nextScene,
  prevScene,
} from "./controller";
export type {
  VnMessage,
  VnScene,
} from "./types";
