/**
 * Alpine.js Components — Entry Point (chat page only)
 */

import "./alpine/types";
import "./alpine/theme";
import "./alpine/i18n";
import "./alpine/app";

// Chat is the only page that still uses Alpine
import "./alpine/chat";

// Event handlers
import "./alpine/htmx";
