// src/config/schema-class/docs.ts — documentation section defaults
import type { DocumentationConfig, } from "../schema";

export const DOCS_DEFAULTS = {
  enabled: true,
} satisfies DocumentationConfig;
