# knip + jscpd research — src/ (2026-09-03, dev @ 7aa33f8b)

Scratch research record (`.tmp/`, git-ignored). Numbers are run-time snapshots —
recompute, never paste into committed docs.

Artifacts in this directory:
- `jscpd-report-full.json` — raw jscpd v5 JSON report, gate mode (4 lines / 30 tokens, migrations + `*.test.ts` ignored)
- `jscpd-analysis.txt` — per-dir/per-file clone ranking, incl. noise-filtered "code-only" section
- `analyze-jscpd.cjs` — the analysis script (re-run against a fresh report with `bun analyze-jscpd.cjs`)

## Tool state at research time

- knip 6.32.2, config `knip.json` (JSONC). Plain run: exit 1 — 1 unused file
  (`scripts/check-parallel-HEAD-probe.mjs`, UNTRACKED leftover in the dev
  checkout, appeared after the 01:18 gate report; NOT part of any worktree fix —
  user should delete it or move to `.tmp/`). 4 config hints.
- src empirically in knip scope: `--debug` "Finding project paths" resolves
  2389 files under `src/` despite the `src/**/*.ts → Refine project pattern
  (no matches)` hint (false positive suspected; A/B-pending).
- jscpd 5.0.16, no config file; flags inline in package.json scripts.
  Gate mode via `check-parallel.mjs` (~L482): runs `bun run jscpd:full`,
  counts `"Clone found"` in stdout, warn-only note.

## Key numbers (snapshot)

- knip full run: 0 unused exports/deps/binaries; 1 unused file; hints: stale
  ignore `tests/e2e/flows/browser/smoke.browser.ts`, stale
  ignoreDependencies `@eslint/js`, stale ignoreBinaries `pip`, spurious
  `src/**/*.ts`.
- jscpd gate mode: 2458 clones, 23,693 dup lines (10.81%), 1837 files.
- jscpd human mode (8/100): 157 clones.
- Noise mass (not actionable code duplication): `src/public/locales/*.json`
  (~51k dup tokens — i18n catalogs are key-parallel by construction),
  `src/public/css` (~17k), `src/frontend/*` vendored bundle (~14k),
  generated files (`src/db/schema-manifest.ts` ~8.7k,
  `src/validation/db-schemas.ts` ~5.3k, `src/test-utils/insert-helpers.ts`
  ~3.6k — regenerated by `db:sync-*`).
- Code-only residual after noise exclusion: 1803 clones / 15.6k dup lines.
- Clone size distribution (gate mode): 1736/2458 clones ≤ 9 lines;
  930 intra-file.

## package.json script defects found

1. `dead:code:ci` = `knip --ci` — `--ci` was removed in knip v6
   ("Unknown option '--ci'"). Script is broken/dead.
2. `jscpd` + `jscpd:full` scan non-code + generated mass → signal loss
   (see Noise mass above). Fix: add ignores + `--gitignore`.
3. Gate ratchet design (check-parallel.mjs, not package.json): 2458-clone
   permanent warning ≈ zero gate value; proposal — JSON report to `.tmp/`,
   warn on increase vs committed baseline. Deferred; scripts-first per user.

## Duplication hotspots (code-only, by dup tokens) — CONFIRMATION QUEUE

NOTE (user directive): dead/duplicated code may be UNFINISHED functionality
that gets connected later — triage keep-vs-wire-vs-remove; nothing is removed
without confirmation.

### routes/* handler boilerplate (dominant family)
- `routes/rpg/*` 11.0k; `routes/admin/*` 8.5k; `routes/actor-items/service.ts`
  5.8k/77 clones (worst single file); `routes/views/*` 5.7k;
  `routes/chats/*` 4.8k; `routes/crafting/*` 3.9k; `routes/nsfw-moderation/*`
  3.4k; `routes/battle/*` 3.2k. Pattern: auth → ownership → validation →
  error-mapping repeats per route file. Proposal: shared route-factory.
- `routes/auth/login.ts` ↔ `register.ts` 315t/73l.
- `routes/character-availability.ts` 1830t/37c, `routes/message-seen.ts`,
  `routes/character-avatars*.ts` same family.

### provider adapters
- `generation/providers/{anthropic,ollama-native,openai-compatible}/index.ts`
  + `http.ts` cross-clones (237t/69l, 216t/55l, 166t/23l).
  Proposal: provider base/factory.

### config schema mirrors
- `config/schema-class/json-schema/generation.ts` ↔
  `config/sections/generation/{sd,llama}.ts` (409t/54l, 342t/37l).
  Proposal: single source + codegen (repo already codegens DB schemas).

### image-edit templates
- `image-edit/templates/builtin/{img2img,inpaint,controlnet,txt2img}.ts`
  (300t/60l, 197t/33l, 187t/49l). Proposal: template registry + overrides.

### invites
- `chat/invites/redeem.ts` ↔ `chat/world-invites/redeem.ts` 216t/23l
  near-exact. Proposal: parameterized shared flow.

### HTML templates (views/components)
- `components/chat/chat-settings-modal.html` ↔ `gm-guidance-panel.html`
  526t/98l (largest code clone); `views/world-edit.html` 1195t/24c,
  `views/admin.html` 1123t/32c. Proposal: htmx partial extraction.

### other files flagged
- `assets/controller/handlers.ts` 1095t/18c; `chat/service/*` 2.8k;
  `services/server-external-manager/{lifecycle,probes}.ts` 220t/50l;
  `config/load/fs.ts` ↔ `config/templates-loader/discovery.ts` 197t/40l;
  `validation/schemas/*`, `config/sections/*` smaller clusters.

## knip dead-code items — CONFIRMATION QUEUE (keep-vs-wire-vs-remove)

- `plugins/**/types.ts` — ignored in knip.json: "Dead scaffolding
  deliberately kept for the plugin extension surface (no consumer yet).
  Tracked: TASK-knip-dead-code." → KEEP (unfinished functionality, per note 1).
- `types` analysis globally excluded in knip.json (test-factory barrels
  re-export `*Factory` types by design) → candidate: restore per-symbol
  `@knipignore` tags instead of global gag (verify tag support in v6 first).
- `scripts/check-parallel-HEAD-probe.mjs` → dev-checkout scratch, delete or
  move to `.tmp/` (user's untracked file — needs user confirm; not committed).
